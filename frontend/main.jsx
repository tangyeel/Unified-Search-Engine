import React from "react";
import ReactDOM from "react-dom/client";
import App from "./tracepath_enhanced.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
